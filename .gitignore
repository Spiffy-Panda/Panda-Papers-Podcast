# Local state — regenerated by init_podcast.py and the dashboard skill
STATUS.json
podcast_generation_state.json

# Local-only project context — discoverable from CLAUDE.md's pointer,
# but kept out of the public repo (candid framings of the public rules
# in PIPELINE-DECISIONS.md, parallel-session notes, sibling-repo deps).
CLAUDE.local.md

# Playwright MCP scratch artifacts (page screenshots + console logs)
.playwright-mcp/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# C# / .NET
bin/
obj/
*.user
.vs/

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp

# Runtime logs
logs/
*.log
